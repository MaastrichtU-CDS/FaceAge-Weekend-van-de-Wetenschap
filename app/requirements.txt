# Dependencies for the FaceAge science fair web service.
#
# Python 3.11 + TensorFlow 2.15 supports ARM64 and x86_64 while retaining
# Keras 2 compatibility with the original FaceAge model.
flask==2.2.5
werkzeug==2.2.3
tensorflow==2.15.1
keras==2.15.0
# MTCNN 0.1.1 imports pkg_resources, removed in newer setuptools releases.
setuptools==80.9.0
# note: mtcnn is installed separately with --no-deps (see Dockerfile.serve)
# because it depends on the GUI opencv-python package, which conflicts with
# opencv-python-headless; its other deps (tensorflow, keras) are pinned here
numpy==1.26.4
h5py==3.10.0
opencv-python-headless==4.10.0.84
Pillow==10.3.0
